# Dinanath Jewellers

This project contains the Flutter app source.

The Codemagic workflow first generates the missing native Android/iOS project folders with `flutter create`, then builds the selected platform.

For iPhone distribution, Apple signing must still be configured in Codemagic with an Apple Developer account/certificate/profile.
